# Repo memory — maestro (rewrite-maestro-in-typescript branch)

- [ADR-0001](adr/0001-typescript-rewrite.md) — TS on bun, bun-shim distribution, immediate `maestro` name takeover, Rust kept as `maestro-legacy`; compiled-binary option rejected (unproven external `.ts` import, spike SIGKILL).
- [ADR-0002](adr/0002-kernel-policy-recipes-separation.md) — three layers: mechanism-only kernel, policy as removable plugins, prompt-first recipes; self-written Cordis-inspired runtime (no cordis dep); no lean mode ever — disable the plugin instead.
- [ADR-0003](adr/0003-single-work-entity.md) — one `work` entity (tree+DAG+kind-as-data+lease) + minimal `decision` entity; supersedes the four-entity clause of ADR-0002; feature lifecycle becomes gates on `work done`.
- [ADR-0004](adr/0004-hook-first-delivery.md) — guidance brief + store mailbox delivered via harness hooks (SendMessage model, own transport); thin mirror pointer lines; recipes on demand; rejected thick HARNESS.md and polled inbox.
- [Bundle 2 recipes](archive/maestro-ts-recipes-policies/) — built-in `recipe` serves 18 source-owned markdown methods on demand and contributes only a brief pointer; `recipe show` never extracts content into repos.
- [Bundle 2 policies](archive/maestro-ts-recipes-policies/) — tdd/qa/research/witness ship disabled and gate through `test:`/`qa:` claim-proof pairs or `research:`/independent-session `witness:` notes; mutation coverage proves witness identity matters.
- [ADR-0005](adr/0005-shared-store-anchor.md) — one repository-unique store shared by worktrees; non-standard Git layouts stay isolated and concurrent IDs/leases use bounded SQLite serialization
- [Bundle 3 shared coordination](archive/maestro-ts-coordination/) — worktrees share the Git-common-root store; cwd fallback accepts only Git's explicit outside-repository diagnostics, while private stage-1 stores and legacy `store.sqlite` remain untouched.
- [Bundle 3 peer identity](archive/maestro-ts-coordination/) — overlap and live-peer output are advisory-only; hook adapters persist Claude/Codex harness identity and Claude-to-live-Claude mail advertises native SendMessage delivery.
- [Bundle 4 cutover](archive/maestro-ts-cutover/) — rewrite CLOSED 2026-08-24: version verb + registry-driven help + stacked-gate combined-pairs hint after joint UAT; main ff d76eb66b -> 6af10da0 (legacy store.sqlite stash-preserved, restored byte-identical); rollout installs live in maestro/astra/synara; nothing pushed; MCP/Cursor/importer deferred.
- [Bundle 1 stage 1+1.1](archive/maestro-ts-rewrite/) — kernel + work/decision/plugin-host/hook delivery shipped and UAT-hardened at 34cd9e8e (evidence pairs, strict positionals, honest plugin lifecycle, portable hooks); closed retroactively 2026-08-24 at cutover.
- [Bundle 5 extensions](archive/maestro-ts-extensions/) — legacy importer (1157 cards/1604 files/842 decisions into read-only legacy_* tables, source byte-identical), MCP two meta-tools (maestro_find/maestro_run) with the dynamic brief as initialize instructions, hookless mirror bootstrap line; Cursor dropped by owner ruling; legacy search bounded to 20 one-line hits + `N more` footer (item 52).
- [Bundle 6 lifecycle](archive/maestro-ts-lifecycle/) — Rust-parity distribution lifecycle: repo uninstall (never deletes data), ff-only update from the recorded source (~/.maestro/source.json), offline drift advisory with MAESTRO_AUTO_UPDATE=0, read-only doctor, push/PR-only CI; ruling: zero RUNTIME deps (pinned @types/bun+typescript devDeps allowed), four legacy tsc diagnostics fixed to zero instead of baselined.
- [Bundle 7 agent-ux](archive/maestro-ts-agent-ux/) — field-test hardening: layered session anchoring (env session ids first, ps-walk, then TTL bridge for ps-blocked sandboxes; anchor-aware liveness, 60-min TTL, passive refresh), tokenized maestro_find with zero-hit hint, self-unblocking LEASE_REQUIRED, nested+positional help, item-52-bounded native search, prefixed recipe examples; SPEC env-claim corrected at acceptance.
- [Bundle 8 field-fixes](archive/maestro-ts-field-fixes/) — two-agent e2e defects fixed by Claude directly: EPERM-correct liveness (sandboxed peer reads no longer expire live leases; ESRCH still immediate), `work cancel --reason` terminal overlay (cancelled_at columns, [cancelled] list, ready exclusion, cancelled blocker resolves dependents), mirror work-lifecycle line, update dirty check ignores untracked wiring (test 64); suite caught 2 regressions (ALTER race, stale committed mirrors) pre-commit.
- [Bundle 9 rollout-fixes](archive/maestro-ts-rollout-fixes/) — install re-execs in the freshly synced runtime when the running code is stale (realpath compare; import.meta.dir resolves symlinks; MAESTRO_INSTALL_REEXEC loop guard), and update treats an ahead-only source as nothing-to-pull with runtime resync (drift advisory converges) while true divergence still refuses.
