# Read-output budget for agent-facing read verbs

2026-06-14  Agent-UX friction observed mid-session (recorded per user; NOT folded into this feature's read-output scope -- candidate separate idea card): maestro link add <me> <peer> and maestro msg send both hard-error when the target card is terminal. Peer card optimize-context-footprint-of-write-command-responses shipped between my 'maestro active' snapshot (which listed it in_progress) and my link attempt seconds later. Two distinct gaps: (1) TOCTOU race -- 'active' surfaces a card + suggests 'link your card to theirs' but the card can be terminal by the time you act, so the suggested command fails; (2) dead-end error -- 'is shipped (terminal); link only live cards' states the rule but offers no recovery path (relates to card no-dead-end-errors / card-c5b8a4). Coordination with a just-finished peer is legitimate (read their shipped decisions, message a follow-up) yet blocked.
2026-06-14  dec-bare-list-defaults-to-live-cards-hide-a25d locked -- Bare list defaults to live cards (hide coarse Closed); --all restores
2026-06-14  dec-list-json-emits-a-compact-single-line-de48 locked -- list --json emits a compact single-line array
2026-06-14  CROSS-CARD OVERLAP found mid-walk (peer session, card dense-json-encoding-for-multi-card-read-contracts, proposed): the peer card explicitly splits the list --json offender by fix-AXIS -- SLICE (which cards appear / slice-bounding) ceded to THIS card; ENCODING (compact array vs NDJSON serialization shape) owned by THEIRS, plus show --compact-json field semantics. RECONCILIATION NEEDED: my locked dec-list-json-emits-a-compact-single-line-de48 (A2) decided the ENCODING shape (compact single-line array) -- that is on the peer's axis, not mine. A1 (slice default; also governs which cards --json emits) is legitimately mine and stands. Plan pending coordination: supersede/withdraw A2's encoding choice in favor of the peer's encoding decision; keep on this card only 'the A1 slice default also governs the --json element set' (format-agnostic). Holding A2 (not superseding unilaterally) until we talk -- they may adopt or reference it. Per user: wait for peer advisor to finish, then link + message to confirm the slice/encoding split and hand encoding back.
2026-06-14  dec-list-json-element-set-follows-a1-slice-f9d2 locked -- list --json element set follows A1 slice; encoding ceded to dense-json sibling
2026-06-14  dec-decision-list-defaults-to-a-recent-n-4a70 locked -- decision list defaults to a recent-N window; --all + --feature escapes
