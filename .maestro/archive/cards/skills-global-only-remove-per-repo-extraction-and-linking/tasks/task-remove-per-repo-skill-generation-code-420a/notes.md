# Remove per-repo skill generation (code + tests)

2026-06-13  Impl plan (advisor-reviewed, 8 pts aligned): DELETE creation path -- skills/extract.rs (whole file), skills/symlink.rs (whole file), skill_symlink_for_agent (install/mod.rs), PreparedMirrors.skill_symlink field + create_skill_symlink trait/impl (mirrors.rs), is_legacy_symlink_only_install (only caller is validate_install_ownership). KEEP for back-compat/migration -- MirrorKind::Symlink + FileOwnership::symlink (lock.rs, deserialize legacy locks), remove_mirrors symlink-removal machinery (prunes legacy symlink per ac-3). ac-1 also requires dropping paths.skills_dir() from init/mod.rs:173 directories vec (it pre-creates .maestro/skills). ac-3 migration: post-commit prune any maestro-owned legacy skills symlink, warn-not-fail; validate_install_ownership tolerates legacy Symlink lock entries. gitignore_block: remove only the two skills lines. Do NOT make removing paths.skills_dir() fn a goal. ac-3 test needs synthetic install-lock fixture (this repo has none); template in tests/install_uninstall_integration.rs.
2026-06-13  Migration design (advisor-reconciled): update (run_update_with_seams) does NOT call install_agent and never rewrites per-agent locks -- it only refreshes global skills + extract_all + binary swap. So the ac-3 prune CANNOT be a post-commit hook inside install. Instead: ONE standalone pub(crate) fn prune_legacy_skill_symlinks(paths) in mirrors.rs, keyed off the lock's MirrorKind::Symlink entries (relative_path + target, no hardcoded agent map), removes the symlink file via remove_symlink_if_owned + drops the lock entry, saves if changed; best-effort (warn, never fail). Called from TWO entry points: top of install_agent_with_writer AND run_update_with_seams (initialized-repo branch). InstallLock::load returns empty() on missing file so no lock = no-op. SCOPE CHOICE: prune iterates ALL agents in the lock (install --agent codex also prunes Claude's legacy symlink) -- correct hygiene, every such symlink is now dead weight and both agents discover globally; simpler than per-agent scoping; ac-3 wording permits it. validate_install_ownership refactor still required independently (uninstall without prior install/update can still see a Symlink entry): filter Symlink-kind out of owned_paths comparison + skip Symlink in per-entry loop; this severs skill_symlink_for_agent + is_legacy_symlink_only_install. VERIFY must prove ac-3 on BOTH update and install entry points (synthetic legacy lock fixture).
2026-06-13  Collateral: repo-local 'maestro sync' shares extract_all, which now covers only hook script + harness (no skills). sync_integration.rs used maestro-card SKILL.md as its drift example -> broke. Retargeted all 3 affected sync tests to record.sh (hook script): it has an independent version gate (HARNESS.md+RECOVERY.md share one gate, so a harness edit refreshes both -> messy count/backup), and capturing the freshly-init'd record.sh as the 'bundled' oracle sidesteps both the @MAESTRO_BIN@ placeholder and any template-visibility import. dry-run test uses HARNESS.md as the 'skip' example. Also updated sync/mod.rs + extraction/mod.rs + init/mod.rs doc comments to drop 'skills' from the resource list. NOT in card's listed test areas but required to keep suite green.
