# resume state readout

2026-06-16  dec-extend-existing-reads-no-new-command-a106 locked -- Extend existing reads; no new command
2026-06-16  dec-tight-slice-park-checkpoints-upstream-1145 locked -- Tight slice; park checkpoints + upstream learnings
2026-06-16  dec-split-dirty-code-other-vs-maestro-cards-cf19 locked -- Split dirty: code/other vs maestro cards
2026-06-16  dec-clean-worktree-note-is-contextual-not-a-1d10 locked -- Clean-worktree note is contextual, not a boolean
2026-06-16  dec-proof-line-concern-only-name-the-exact-c571 locked -- Proof line concern-only; name the exact repair
2026-06-16  dec-installed-binary-field-omit-from-resume-429b locked -- Installed-binary field: omit from resume/status, passive check owns it
2026-06-16  dec-ac-7-ship-gate-proof-recheck-widen-08ca locked -- ac-7 ship-gate proof recheck: widen slice, gate on contract_hash drift only
2026-06-16  dec-ac-7-final-non-blocking-ship-time-576f locked -- ac-7 final: non-blocking ship-time advisory on verified_commit staleness
2026-06-16  2026-06-16 build authorized by user (chose 'Hand off to build' at design->card boundary). Constraints already in contract: binary field omitted (dec-...-429b), no new command (card-36759d), ac-7 is a non-blocking ship --dry-run advisory on verified_commit staleness (dec-...-576f supersedes the contract_hash gate). Implement test-first per tdd.md.
2026-06-16  Release-local (cargo build --release + cp to ~/.local/bin) deferred to after the final implementation task, before qa-slice e2e: no manual binary testing happens between intermediate task commits, and the qa-slice replay needs the newest binary. Each verified task is committed locally on branch resume-state-readout (code only; .maestro/ card store stays untracked per repo convention).
2026-06-16  T2 design (advisor-reviewed): no refactor of next_action_for; follow the existing resume/status next-action duplication pattern. Two per-surface clean-note predicates over CHEAP inputs (not TaskRecord): resume keys on task.state + has_blockers + feature(status,total,verified) mirroring next_action_for ship/verify arms (InProgress/NeedsVerification, or feature InProgress all-verified); status keys on next_action.kind in {complete_task,proof_recovery} OR ready_to_ship_features non-empty (feature_ship is never a status next_action.kind; it lives in ready_to_ship_features). feature_ship arm kept on resume because reachable via 'maestro resume --feature <id>'. Git line + clean note rendered identically on both surfaces. Gate = RED integration test through the real binary in tests/status_next_integration.rs (needs a REAL git repo: setup_repo only makes a .git marker so snapshot()=Err and the line is absent there); predicate unit test is a supplementary add-on. Wording: 'git: <branch|detached>, <C> code/other + <M> maestro-card uncommitted'; note: 'note: commit the <C> uncommitted code/other change(s) before the ship/verify step so proof reflects the intended tree'.
2026-06-16  T2 committed locally 0356b5f1 on resume-state-readout (code+test only; .maestro untracked). Auto-verify passed -> 2/4 tasks verified. Full suite 887 passed/0 failed across 53 binaries; fmt+clippy clean. Wording shipped: 'git: <branch|detached>, <C> code/other + <M> maestro-card uncommitted' and note 'commit the <C> uncommitted code/other change(s) before the ship/verify step so proof reflects the intended tree'.
2026-06-16  T3 design (ac-4/5/6): render ONE concern-only proof line for the focal task, adjacent to next:. Focal = resume.selected_task / status.next_action task. Pure predicate proof_concern_line_from(task_id,kind,state,stale_reasons): Stale->stale_proof_repair (refresh framing, HEAD moved likely no code change, with old7->new7 when verified_commit drift present, else bare); Failed->'proof: failed; fix, then re-verify: maestro task verify <id>'; Missing only when state==NeedsVerification->'proof: missing; bind proof: maestro task verify <id>'; Accepted/other->None. Two CLI flips: resume.rs:244 and status.rs:548 (query proof -> task verify, + status reason text); resume.rs:292 read-slot query proof KEPT. Concern line is render-only (#[serde(skip)]). SCOPE NOTE (advisor-confirmed): Stale requires state Verified, which is never auto-selected as focal on default resume/status, so Stale surfaces only via 'maestro resume --task <verified-stale-id>'; Failed/Missing surface on default resume+status (NeedsVerification auto-selected). To be proven at ship and confirmed with user.
2026-06-16  T3 implemented (TDD red->green). RED: 2 integration tests in tests/status_next_integration.rs (failed-proof on resume+status; verified-stale via resume --task + fresh-negative) + 5 unit tests in src/interfaces/cli/mod.rs#tests. GREEN: helpers in cli/mod.rs (short_commit, stale_proof_repair [pub for T4 reuse], proof_concern_line_from [pure predicate], proof_concern_line [pre-gates state in {NeedsVerification,Verified}, soft proof_status read]); render-only proof_concern field (#[serde(skip)]) on ResumeReport + StatusReport so JSON contract unchanged; shared focal_proof_concern() dedups status' two builders (build_status_report + build_task_next_report). Flips: resume.rs next-action query proof->task verify (ac-6); status.rs proof_recovery argv query proof->task verify + reason 'verification needs proof; re-verify' (kept internal kind 'proof_recovery', referenced at status.rs:480 for ship/verify detection). DECISIONS: (1) import fix - use crate::domain::{proof,task} collided with local 'pub mod task' (E0255); imported proof module + TaskState/TaskRecord types directly. (2) Accepted redundancy: Failed/Missing concern line and flipped next both name 'task verify <id>'; concern line's distinct value is the classification + nuanced repair (failed=fix-then-verify, missing=bind, stale=refresh). gates: fmt clean, clippy --all-targets -D warnings exit 0, unit 5/5 + integration 2/2 green; full suite running.
2026-06-16  SHIP-TIME proof obligations (advisor-reinforced): ac-4 prove all THREE: (a) concern surfaces for failed+missing (resume+status), (b) NO line for accepted/fresh, (c) state explicitly that Stale is unreachable on default resume/status (next_action never selects a Verified task) and surfaces only via 'maestro resume --task <verified-stale-id>' -- user must ratify this limitation in the --prove evidence, do not bury. ac-6 prove against the NEXT-ACTION line specifically (resume 'recover proof with maestro task verify'; status next_action argv 'maestro task verify') and note resume required_reads STILL contains 'maestro query proof' (retained read-slot, line 292) so the kept read is not misread as an incomplete flip. release-local is AFTER T4 (final impl), not after T3 commit. At T3 commit: git add the 4 code files then eyeball git status to confirm card-a177b3 + resume-prior-task-context + resume-state-readout card churn did NOT ride along.
2026-06-16  T4 (ac-7) implemented TDD red->green. Decisions: (1) advisory computed in domain feature::ship dry-run branch and APPENDED to report.note (not a new TransitionReport field) -- minimal, no touching every TransitionReport constructor, and the note prints immediately above the 'ship preview:' block the locked decision cited (feature.rs:640-649). (2) drift = direct string compare of stored verified_commit vs git::head (both full OIDs from the same source; identical to proof stale.rs compare) -- avoids a per-task proof_status call, self-contained; contract_hash drift intentionally NOT handled (provably frozen for verified tasks per dec-ac-7-final). (3) non-blocking guaranteed structurally: advisory only ever appended to the note string, never to gaps/ship_gaps_for_record -- test confirms 'would ship' survives both before and after HEAD moves. (4) wording = locked canonical string 'N child task(s) verified at older commits (HEAD moved); re-verify if their code changed: <ids>' prefixed 'advisory (does not block ship): '. (5) appended unconditionally (both would-ship and would-block notes); drift fact is independent of the gate. Gates: fmt clean, clippy --all-targets -D warnings exit 0, T4 integration test green, full suite running.
2026-06-16  SHIP-TIME RATIFICATION (ac-7 preview placement): dec-ac-7-final carried a preview sketch placing the advisory NESTED inside the 'ship preview:' block (the feature.rs:640-649 it cited) with a trailing '(advisory; does not block ship)'. Shipped placement instead prints the advisory as a line ABOVE 'ship preview:' (appended to report.note), prefixed 'advisory (does not block ship): '. The ac-7 checks test wording substrings only ('verified at older commits', 're-verify if their code changed', no blocking gap), so the contract passes either way -- but this is a deliberate deviation from the locked preview's NESTING, and the user should ratify it at ship. If the user prefers the nested sketch: make verified_child_commit_drift pub(crate), print it from the CLI dry-run block at feature.rs:640-649, drop the note-append (no JSON impact -- feature ship has no --json and TransitionReport is not Serialize). Cheap to switch; does not block.
2026-06-16  T4 verify: first auto-verify cargo test hit a transient exit 101 (panic at status_next_integration.rs:467, a T2 test asserting absence of a git line) -- caused by two concurrent cargo test processes racing the shared test binaries (my earlier background full-suite finishing + the gate's own cargo test). Re-run of the full suite with nothing else competing was clean (exit 0, 53/53 result-ok blocks, 0 failed). Re-ran maestro task verify isolated -> PASSED. No code defect; the race is a test-harness concurrency artifact, not a flaky test in the resume-state-readout changes.
2026-06-16  ac-7 placement realignment: the T4 advisory was committed (6b9c5522) appended to report.note in domain registry::ship dry-run, printing ABOVE the 'ship preview:' header. The locked decision dec-ac-7-final-non-blocking-ship-time-576f specifies it prints INSIDE the ship preview block (feature.rs:640-649) as a 'note:' line. Realigned to the SPEC rather than amend the decision (VERIFY gates on delivered==locked, user away): made verified_child_commit_drift pub + re-exported from feature mod; dropped the registry note-append; CLI ship_feature dry-run now prints '  note: N child task(s) verified at older commits (HEAD moved); re-verify if their code changed: <ids> (advisory; does not block ship)' right after '  feature:'. T4 integration test is a real-binary stdout substring assertion (not report.note), stays green. No JSON/gaps impact.
2026-06-16  SHIPPED 2026-06-16 (local, branch resume-state-readout, NOT pushed). All 7 ACs covered via qa.md counting slices (bl-001..bl-007), real-binary evidence captured in /tmp/ev_*.txt against g6bebcfa5. ac-7 demonstrated live on this feature's own ship --dry-run: 'would ship' + non-blocking advisory listing all 4 verified children (proofs predate HEAD), rendered as a note: line inside the ship preview block. feature ship ran full stack.verify (fmt+clippy+273 unit+integration+doctests) -> all green -> status=shipped. 2 code commits: 6b9c5522 (T4) + 6bebcfa5 (ac-7 placement realignment to locked dec-...-576f).
