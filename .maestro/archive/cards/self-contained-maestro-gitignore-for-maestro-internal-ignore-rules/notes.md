# Self-contained .maestro/.gitignore for maestro-internal ignore rules

2026-06-15  dec-gitignore-layout-hybrid-maestro-0cd0 locked -- Gitignore layout: hybrid (.maestro/.gitignore + shrunken root block)
2026-06-15  dec-gitignore-scope-maestro-scope-only-2fcf locked -- Gitignore scope: maestro-scope-only (single .maestro/.gitignore, no root block)
2026-06-15  Impl decision log started. Loose impl Q resolved during work: reuse GitignoreSection managed-block machinery (repoint single .gitignore mirror to .maestro/.gitignore + shrink gitignore_block body) rather than a new wholesale MirrorKind -- least code, reuses upsert/remove. Migration: strip legacy root block on sync.
2026-06-15  2026-06-15 authorization: user said 'ship this as new feat for other maestro user', then refined scope to 'maestro scope only, dont care about .claude or codex' and chose Drop both. Authorized to build + ship. Constraint: maestro's gitignore footprint = .maestro/ only; no root block; agent paths dropped (captured as non-goals + dec-...-2fcf).
2026-06-15  IMPL decision (entry point): the .maestro/.gitignore write AND the legacy-root-block strip are owned exclusively by 'maestro install --agent' (install_agent -> mirror_plan + lock-recorded ownership). Verified init/sync/upgrade all structurally leave the gitignore mirror untouched; sync creating it would orphan it from lock-driven uninstall on the preserved-.maestro/ path. Consequence (architecture-forced, non-blocking): existing repos migrate only when the user re-runs 'maestro install'; sync/upgrade do NOT strip the legacy block. Harmless (old root block keeps ignoring until then). Surface as a one-line ship note. Contract fix: ac-1/ac-2 'init/sync' wording is imprecise-not-false (documented setup runs install) -> prove via 'maestro install' at verify; ac-3 ('sync removes the block') is substantively false -> waive at verify + add install-worded migration AC via amend.
2026-06-15  IMPL landed (T1+T2): (1) mirror repointed '.gitignore' -> '.maestro/.gitignore' in mirror_plan (mirrors.rs:139). (2) gitignore_block() shrunk to maestro-internal paths RELATIVE to .maestro/ (no prefix) + added 'update-check', dropped the two agent lines + 'Local agent settings' header (mirrors.rs:978). (3) migrate_legacy_root_gitignore(paths) added (mirrors.rs) + called at the END of install_agent_with_writer, AFTER mirror write + lock commit (mod.rs:101) so no un-ignore window. DECISION on empty-residue: the migration strips the maestro HashComment block from root .gitignore; if the strip empties the file it DELETES the root .gitignore (keyed on emptiness alone, NOT created_fresh -- an emptied file had no user content to preserve, mirroring is_empty_residue husk semantics); else writes the remainder back; no-op if absent or no block. Uninstall already removes .maestro/.gitignore via the lock husk-path (created_fresh + is_empty_residue) even when .maestro/ is preserved. Tests: install_mirrors.rs path/body assertions updated; 3 new integration tests (fresh-write+no-root-block, legacy-block-migration+preserve-user-lines, empty-residue-delete); repointed shared-mirror + removing-retry + no-partial-write tests to .maestro/.gitignore. install suites 40/40 green.
