# monorepo and multi-service maestro scopes

2026-06-16  dec-foundational-model-one-root-scope-a225 locked -- Foundational model: one root scope + filterable project dimension; no polyrepo federation
2026-06-16  dec-project-tagging-auto-infer-from-folder-db98 locked -- Project tagging: auto-infer from folder, overridable, first-segment default + optional layout globs
2026-06-16  dec-project-is-a-new-card-base-field-4aca locked -- project is a new Card-base field, distinct from feature affected_areas
2026-06-16  dec-read-surface-project-filter-project-3d10 locked -- Read surface: --project filter + [project] badge + group-by-project in unfiltered list
2026-06-16  dec-activation-declaration-gated-auto-infer-7cd8 locked -- Activation: declaration-gated auto-infer via projects: layout in harness config
2026-06-16  dec-build-sequencing-full-surface-in-one-dd0e locked -- Build sequencing: full surface in one pass (no thin-slice deferral)
2026-06-16  dec-setup-context-ingestion-read-docs-specs-16ec locked -- Setup context ingestion: read docs+specs IN, synthesize into root harness guidance per-project (no write-out)
2026-06-16  2026-06-16 user authorized full-surface build ('build it'). Per dec-build-sequencing-full-surface-in-one-dd0e: land all 15 ACs in one pass. Constraints carried from design: read-in only for setup context (write-out to sub-project specs deferred per open question, do NOT implement); maestro stays passive (no new CLI verb or harness schema field for context ingestion); undeclared repos byte-identical to pre-feature binary; bounded doc set (AGENTS.md/CLAUDE.md/README.md/docs/*.md + depth/size cap).
2026-06-16  T4 impl interpretation of frozen ACs: [project] badge keys off per-card project presence (ac-5, unconditional); grouping headers key off >=2-distinct threshold (ac-6). The ac-3 byte-identical guarantee is verified on the zero-stored-project (undeclared) case: project_badge returns empty string so project-less rows are unchanged. A 1-distinct declared case is flat but DOES show badges (expected). Also: list/ready/status --json surface project via read-time projections on TaskRecord (#[serde(skip)], not persisted) and the non-persisted FeatureView; no card schema change.
2026-06-16  VERIFY wording reconciliations (3, all PASS substantively): (1) ac-1 says '.maestro/cards/<id>/card.yaml' but real storage path varies by type - task/bug/chore at .maestro/cards/tasks/<id>/task.yaml, idea/decision in aggregate ideas.yaml/decisions.yaml; substantive claim (project persists round-trip for every type) holds. (2) ac-6 'byte-identical to today' holds for grouping structure (no headers at <2 distinct) but the unconditional ac-5 badge appears at 1-distinct; byte-identity to pre-feature is the ac-3 no-declaration bar, ac-6 governs only the grouping threshold. (3) ac-10 says gate is 'acceptance OR affected_areas' but implemented gate requires BOTH (AND) plus qa-baseline as independent gaps; the D3 no-interaction claim (project independent of gate) holds either way.
