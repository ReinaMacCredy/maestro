# Notes

2026-06-12  [card-942982] Closed by card-model branch hardening commit fb59f7b8: harness flat-card drops and container migration cleanup now delete through snapshot CAS, and card migration run-event rewrites now reject stale append-only log snapshots.
2026-06-12  [card-3cb8a9] Adopted into feature skill-time-cli-discovery-agents-still-probe-help as dec-one-shot-decision-open-lock-c2d0 (locked 2026-06-12); implement there.
2026-06-12  [idea-prepare-coverage-warning-suggests-an-c3a3] Second friction same session: after abandoning the uncovered tasks, prepare --from refuses (feature already has child tasks) and abandoned tasks cannot detach (--no-feature rejected on terminal). prepare should ignore terminal children or offer --append; recovery required manual create/set-covers/explore/accept per task.
2026-06-14  [idea-link-msg-active-toctou-on-terminal-cards-39a9] DESIGN LOCKED 2026-06-14 (recorded as a note: decision cards can't attach to an idea card; promote to a feature when built). Chosen: KEEP the live-only invariant for link add / msg send (terminal cards stay unlinkable), and fix the two UX defects. (1) The link-to-terminal error (src/domain/card/edit.rs) and msg-to-unlinked error (src/interfaces/cli/msg.rs) are untyped bail! strings -- inline a recovery line ('X is finished; read it: maestro show X / maestro decision list --feature X') so neither dead-ends; when msg's target is terminal, point at the read path, not the link command that would also fail. (2) active.rs re-checks each peer's liveness when rendering the coordination tip: LIVE peer -> 'maestro link add <you> <their>'; TERMINAL peer -> 'maestro decision list --feature <their>' (read), never the doomed link command. Implementation: inline fix: lines in the bail! strings (consistent with the hand-rolled fix: at task/doctor.rs:152), NOT typed-variant conversion (no-dead-end-errors shipped 2026-06-07 deliberately excluded the ~150 untyped sites; these are in that set). REJECTED: grace window (new 'recently terminal is linkable' concept + window/expiry edges) and session-based messaging (couples msg to session liveness; niche).
