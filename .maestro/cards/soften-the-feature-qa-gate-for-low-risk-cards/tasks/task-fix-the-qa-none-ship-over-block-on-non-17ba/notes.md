# Fix the qa:none ship over-block on non-behavioral amends

2026-06-16  Fix: extracted qa::qa_declared_none_fresh(qa, amends) as a pure fn (mirrors ship_qa_gaps's pure-gate design; unit-testable in isolation) and repointed the single ship-gate call site (registry.rs:844). Root cause: the freshness was count-based (amend_log_position == amends.len()), so ANY amend staled the qa:none waiver; aligned it to ship_qa_gaps E.1 -- waiver holds unless a behavioral amend (is_behavioral: added acceptance/area) lands at/after the declaration position; out-of-range position -> 0 (fail-closed). Regression check: the existing CLI test qa_none_accept_skips_gates_until_a_behavioral_amend... uses --add-area (behavioral), so my fix PRESERVES it (behavioral amend still re-arms + re-declare still ships). The qa:none + non-behavioral-amend case had NO guard, so added CLI test qa_none_survives_a_non_behavioral_amend_without_redeclaring ([bl-001]) + 4 unit tests. RED proven at unit level (qa_none_survives_non_behavioral_amend failed pre-fix) and earlier live on the old binary (--add-non-goal -> 'qa-baseline missing').
