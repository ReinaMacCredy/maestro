// Reserved for future port adapters (L7.x).
export interface DeployServices {
  // Reserved for future port adapters (L7.x deploy gate, etc.).
}

export function buildDeployServices(): DeployServices {
  return {};
}
