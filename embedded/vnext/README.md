# Maestro vNext candidate resources

These files are Stage-0 candidate inputs only. They are deliberately outside
the current extraction and installation registries. Nothing below this
directory is runtime-active, installable, authoritative, or a source of next
action until a complete candidate Contract, Resource/Bundle/Release identity
closure, publication mandate, and cutover gate all succeed.

The legacy
`embedded/loop-recipes/profiles/maestro.standard-six-phase.v1.yml` file is
migration/proof-only evidence. It is not a vNext profile. The only vNext
Bounded Continuation profiles are `attended` and `unattended`.
