commands:
  install: bun install
  test: bun test
  typecheck: bun run typecheck
  build: bun run build
  lint: echo "No linter configured"
  missionControlJson: bun run src/index.ts mission-control --json
  missionControlPreview: bun run src/index.ts mission-control --preview
  sessionJson: bun run src/index.ts session --json
  releaseLocal: bun run release:local

services: {}
