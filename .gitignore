# >>> maestro >>>
# Maestro local agent settings
.claude/settings.local.json
.codex/hooks.json
.factory/hooks.json
# <<< maestro <<<

# --- user-managed (outside the maestro block) ---

# Rust build artifacts
/target/
**/*.rs.bk

# TypeScript/OpenTUI dependencies
/node_modules/

# Maestro local cache (not covered by the managed block above)
.maestro/
# macOS / local noise
.DS_Store
.claude/scheduled_tasks.lock
